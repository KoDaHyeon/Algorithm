#include <iostream>
#include <vector>

using namespace std;

int main(void)
{
  int test_case; //테스트 케이스 개수
  cin >> test_case;
  for(int i=0; i<test_case; i++){
    int friendship; //친구 관계 수
    vector<int> p;
    cin >> friendship;
    for(int j=0; j<friendship; j++){
      string a, b; //친구 아이디
      vector<int> rank;
      cin >> a >> b;
      p.emplace_back()

      //union
      // a<-b (a가 b의 부모관계) 부모는 여럿일 수 있음-> 벡터의 벡터..?
      //두 집합을 합칠 때 집합의 크기: 
      //집합1
    }
      

  }
}